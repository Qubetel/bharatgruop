# Deny access to config file
<Files "config.php">
    Order Allow,Deny
    Deny from all
</Files>

# Enable URL rewriting
RewriteEngine On

# Handle API requests
RewriteRule ^contact$ api/contact.php [L]
